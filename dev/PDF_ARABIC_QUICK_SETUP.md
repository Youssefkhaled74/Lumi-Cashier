# ⚡ دليل الإعداد السريع - دعم العربية في PDF

## 📋 خطوات التنفيذ (5 دقائق)

### الخطوة 1️⃣: التحقق من الإعدادات ✅

الإعدادات جاهزة ومطبقة! تم تحديث:
- ✅ `config/dompdf.php` - تم تفعيل دعم العربية
- ✅ `app/Services/PdfGenerator.php` - تم تحسينه للـ RTL
- ✅ مجلدات الخطوط تم إنشاؤها

### الخطوة 2️⃣: اختبار الحل الحالي 🧪

```powershell
# افتح المتصفح واذهب إلى:
http://localhost/pdf-test-ar
```

**المتوقع**: ملف PDF بنص عربي واضح ومقروء باستخدام خط **DejaVu Sans** المدمج.

---

## 🎨 تحسينات اختيارية (للحصول على خطوط أجمل)

### الخيار 1️⃣: استخدام DejaVu Sans فقط (موصى به للبداية)

✅ **لا حاجة لأي إعدادات إضافية!**  
DejaVu Sans مدمج مع DomPDF ويدعم العربية بشكل جيد.

### الخيار 2️⃣: إضافة خطوط عربية مخصصة (اختياري)

#### 1. تحميل الخطوط

اختر خط واحد أو أكثر:

##### **Amiri** (خط نسخ تقليدي - موصى به) ⭐
```powershell
# 1. افتح المتصفح واذهب إلى:
https://github.com/aliftype/amiri/releases/latest

# 2. حمّل ملف amiri-[VERSION].zip
# 3. استخرج الملفات من المجلد ttf/
# 4. انسخ هذه الملفات إلى www/public/fonts/:
#    - Amiri-Regular.ttf
#    - Amiri-Bold.ttf
```

##### **Cairo** (خط عصري)
```powershell
# 1. اذهب إلى:
https://fonts.google.com/specimen/Cairo

# 2. اضغط "Download family"
# 3. استخرج الملفات واختر:
#    - Cairo-Regular.ttf
#    - Cairo-Bold.ttf
# 4. انسخها إلى www/public/fonts/
```

##### **Tajawal** (خط كوفي حديث)
```powershell
# 1. اذهب إلى:
https://fonts.google.com/specimen/Tajawal

# 2. اضغط "Download family"
# 3. استخرج الملفات واختر:
#    - Tajawal-Regular.ttf
#    - Tajawal-Bold.ttf
# 4. انسخها إلى www/public/fonts/
```

#### 2. استخدام الخطوط في القوالب

أضف هذا في ملف Blade الخاص بالـ PDF:

```html
<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <style>
        @font-face {
            font-family: 'Amiri';
            src: url('{{ public_path("fonts/Amiri-Regular.ttf") }}') format('truetype');
            font-weight: normal;
        }
        
        @font-face {
            font-family: 'Amiri';
            src: url('{{ public_path("fonts/Amiri-Bold.ttf") }}') format('truetype');
            font-weight: bold;
        }
        
        body {
            font-family: 'Amiri', 'DejaVu Sans', sans-serif;
            direction: rtl;
            text-align: right;
        }
    </style>
</head>
<body>
    <!-- محتوى الفاتورة -->
</body>
</html>
```

---

## 🔍 اختبار الحل

### اختبار 1: PDF بسيط

```powershell
# افتح:
http://localhost/pdf-test-ar
```

### اختبار 2: فاتورة حقيقية

```php
// في routes/web.php (للاختبار السريع)
Route::get('/test-invoice', function () {
    return app(\App\Services\PdfGenerator::class)->streamHtml(
        '<h1 style="direction:rtl; text-align:right;">فاتورة اختبار</h1>
         <p style="direction:rtl; text-align:right;">المبلغ: 100 جنيه</p>',
        'test-invoice.pdf'
    );
});
```

ثم افتح: `http://localhost/test-invoice`

---

## ✅ قائمة التحقق

- [ ] التحقق من أن `config/dompdf.php` يحتوي على `'default_font' => 'dejavusans'`
- [ ] التحقق من أن `config/dompdf.php` يحتوي على `'enable_font_subsetting' => true`
- [ ] اختبار `/pdf-test-ar` - يجب أن يظهر النص العربي بوضوح
- [ ] (اختياري) تحميل ونسخ خطوط عربية إلى `public/fonts/`
- [ ] (اختياري) تحديث قوالب PDF لاستخدام الخطوط الجديدة

---

## 🎯 النتيجة المتوقعة

### بدون خطوط مخصصة (DejaVu Sans):
✅ نص عربي واضح ومقروء  
✅ حروف متصلة بشكل صحيح  
✅ اتجاه من اليمين لليسار  
⚠️ جودة الخط "جيدة" وليست "ممتازة"

### مع خطوط مخصصة (Amiri / Cairo):
✅ نص عربي بجودة احترافية  
✅ حروف جميلة ومتقنة  
✅ دعم كامل للتشكيل  
✅ مثالي للطباعة والعرض

---

## 🆘 حل المشاكل

### المشكلة: علامات استفهام بدلاً من العربية

```powershell
# 1. تحقق من الإعدادات
# تأكد من أن config/dompdf.php يحتوي على:
'default_font' => 'dejavusans'

# 2. امسح الـ cache
cd www
php artisan config:clear
php artisan cache:clear
```

### المشكلة: النص معكوس

```html
<!-- تأكد من وجود هذا في الـ HTML: -->
<html dir="rtl">
<body style="direction: rtl; text-align: right;">
```

### المشكلة: الخط المخصص لا يعمل

```powershell
# 1. تحقق من المسار
# تأكد من أن الملفات موجودة في:
www/public/fonts/YourFont.ttf

# 2. تحقق من الصلاحيات
# تأكد من أن المجلد قابل للقراءة

# 3. تحقق من الإعداد
# تأكد من أن 'isRemoteEnabled' => true في config/dompdf.php
```

---

## 📚 مراجع

- **الدليل الشامل**: `dev/PDF_ARABIC_COMPLETE_GUIDE.md`
- **أمثلة الخطوط**: `public/fonts/README.md`
- **كود الاختبار**: `app/Http/Controllers/PdfTestController.php`

---

## 💡 نصائح

1. **ابدأ بـ DejaVu Sans**: لا تعقّد الأمور، استخدم الخط المدمج أولاً
2. **اختبر قبل التحميل**: جرّب `/pdf-test-ar` قبل تحميل خطوط إضافية
3. **استخدم Font Subsetting**: مفعّل بشكل افتراضي لتقليل حجم الملف
4. **الأرقام LTR**: استخدم `direction: ltr` للأرقام لقراءة أفضل

---

**وقت التنفيذ الإجمالي**: 2-5 دقائق  
**المتطلبات**: لا شيء (كل شيء جاهز!)  
**الحالة**: ✅ جاهز للاستخدام

---

تم بواسطة GitHub Copilot | نوفمبر 2025
